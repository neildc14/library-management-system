<nav class="col col-sm-2 sidebar-nav">
  <ul
    class="nav flex-column nav-pills border bg-light py-1"
    id="v-pills-tab"
    role="tablist"
    aria-orientation="vertical"
  >
    <%navlinks.forEach((nav)=>{%>
    <li class="m-1">
      <a
        class="nav-link btn btn-nav <%= active === nav._url ? 'active' : '' %>"
        role="tab"
        aria-controls="v-pills-home"
        aria-selected="true"
        href="<%=nav._url%>"
        ><%=nav._nav%></a
      >
    </li>
    <%if(nav._nav === "All Book Instances") {%>
    <div class="line"></div>
    <%}%> <%})%>
  </ul>
</nav>
